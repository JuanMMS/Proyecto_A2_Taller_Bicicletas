package controller;

import model.Taller;

public class OrdenServicioController {
    Taller taller;
    public OrdenServicioController(Taller taller) { this.taller = taller;}
    public boolean crearOrdenServicio(Taller taller) {return taller.}
}
